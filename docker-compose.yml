services:
  pg:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: datapack
      POSTGRES_USER: myuser
      POSTGRES_PASSWORD: mypassword
    ports:
      - "54321:5432"
    volumes:
      - "pgdata_fresh:/var/lib/postgresql/data"
volumes:
  pgdata_fresh: